package com.unicauca.carrito.changelogs;
/*
import com.github.mongobee.changeset.ChangeLog;
import com.github.mongobee.changeset.ChangeSet;
import com.mongodb.client.MongoCollection;
import com.mongodb.client.MongoDatabase;
import com.unicauca.carrito.domain.model.Categoria;
import org.springframework.data.mongodb.core.mapping.Document;

@ChangeLog
public class DBChangelog {

    @ChangeSet(order="001",id="change1",author="testMe")
    public void insertingCategories(MongoDatabase db){
        MongoCollection mycollection = db.getCollection("categoria");
        Categoria doc = new Categoria("testName", true);
        mycollection.insertOne(doc);
    }
} */
